Landing Page Component Overhaul — ARGON AI
Integrate 6 premium 21st.dev components into the landing page, replacing existing sections and adding new visual elements.

Current Section Order

Hero → Features → Testimonials → Pricing → FAQs → CTA → Footer
Proposed Section Order (after changes)

Hero (Pulse Beams Get Started, no Live Demo) → Features (Spotlight Cards) → Testimonials (Columns) → Pricing (Creative Pricing + Credit Card Hover) → FAQs → CTA → Wave Path → Footer
Phase 1 — Testimonials Columns + Spotlight Feature Cards
Scope: Replace the two simplest sections that don't affect other components.

[MODIFY] 
testimonials.tsx
Install npx shadcn@latest add https://21st.dev/r/sshahaider/testimonials-columns-1
Replace the current masonry grid with the new TestimonialsColumns component
Keep existing testimonial data (6 testimonials), map them into the new component's format
Preserve id="testimonials" for the IntersectionObserver nav dot
[MODIFY] 
features.tsx
Install npx shadcn@latest add https://21st.dev/r/easemize/spotlight-card
Wrap each feature card in the SpotlightCard component for the mouse-following spotlight effect
Keep existing feature data and layout structure
Preserve id="features" for the IntersectionObserver nav dot
Phase 2 — Hero: Pulse Beams Get Started Button
Scope: Replace the hero CTA area.

[MODIFY] 
landing-client.tsx
Install npx shadcn@latest add https://21st.dev/r/aceternity/pulse-beams
Replace the current .hero-sub div containing "Get Started" + "Live Demo" buttons
Remove the "Live Demo" button entirely
Wrap the "Get Started" button inside the PulseBeams component for animated beam lines
[MODIFY] 
landing.css
Remove .hero-demo-btn styles (button deleted)
Phase 3 — Creative Pricing + Credit Card Hero Hover
Scope: Replace the pricing section entirely.

[MODIFY] 
pricing.tsx
Install npx shadcn@latest add https://21st.dev/r/kokonutd/creative-pricing
Install npx shadcn@latest add https://21st.dev/r/ruixen.ui/credit-card-hero
Replace current pricing cards grid with the CreativePricing component
Add motion-sensitive CreditCardHero hover cards as a visual above the pricing section
Keep existing pricing data (Free / Pro / Enterprise), map into new format
Preserve id="pricing" for the IntersectionObserver nav dot
Phase 4 — Wave Path between CTA and Footer
Scope: Add a purely decorative animated section divider.

[MODIFY] 
landing-client.tsx
Install npx shadcn@latest add https://21st.dev/r/sshahaider/wave-path
Insert <WavePath /> component between <CTA /> and <Footer />
No data changes needed — purely decorative
Phase 5 — Polish & Rename
Update all remaining "Locus" references to "ARGON AI" across CTA, Footer, and any other components
Test all sections in both light and dark themes
Verify IntersectionObserver nav dots still work for all sections
Run pnpm run dev to confirm no build errors
Verification Plan
Manual Verification
Check each section renders correctly in both light and dark mode
Verify IntersectionObserver nav dots still highlight correctly when scrolling
Test hover effects on Spotlight Cards and Credit Card Hero
Confirm Pulse Beams animation plays on the Get Started button
Verify Wave Path animates between CTA and Footer
Check no console errors in DevTools
Build Check
pnpm run dev — confirm no TypeScript or build errors